import { PageMeta } from 'belge';

<PageMeta
  title="Installation"
/>

Installation
============

Install gridual with npm:

```bash
npm install gridual --save
```

Now you can use Container, Row and Col components like this:

```javascript
import { Container, Row, Col } from 'gridual';
```
